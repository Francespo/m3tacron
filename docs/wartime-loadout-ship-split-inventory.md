# Inventory: BTA-NR2 Y-Wing "Wartime Loadout" XWA ship split

- Task: `8c9aa3209522`
- Date: 2026-09-18
- Type: **read-only inventory + report. No functional, data, schema, seed or migration change.**
- Status: findings for review; fixes intentionally deferred.

## 1. Summary

XWA split the Resistance **BTA-NR2 Y-Wing** into two chassis:

- `btanr2ywing` — the original Y-Wing; its pilots have the **Intuitive Interface** ship ability and `Wartime Loadout` remains a separate configuration upgrade.
- `btanr2wywing` — a second Y-Wing entry; its pilots have the upgrade's effect baked in as the **Devastating Barrage** ship ability, plus 2 extra shields and a white Reload action.

The XWA source shipped the split as **pilot variants with a `-wartime` suffix pointing at a second ship xws**. m3tacron stores and resolves XWS ids in several places, but the split is not handled consistently anywhere: historical and new XWS forms of the same real-world squadron produce different identities, the new chassis has no pilot→ship mapping, and the new ship xws has no icon glyph. This report inventories the impact and lists candidate fixes without implementing them.

## 2. Scope and method

Read-only inspection of the worktree at base `main` (HEAD `23d033f`), plus:

- reading the vendored game data (`external_data/` git submodule, commit `3bb6b58`) and the prebuilt frontend manifest (`frontend/static/data-xwa/xwing-data.json`);
- executing the real backend lookup/parse code against that data (commands in §10);
- read-only SQL against the live dev database (`m3tacron_staging` on `84.8.253.2`) for stored/archived squads.

No file outside this report was modified. The `external_data` submodule working tree was populated with `git submodule update --init --recursive` so the real data could be read; no tracked repository file changed.

## 3. Where ship and upgrade data lives

| Layer | Path / table | Identity used |
|---|---|---|
| Vendored game data (source of truth) | `external_data/xwing-data2/data/` (XWA) and `external_data/xwing-data2-legacy/data/` (Legacy); git submodule → `Francespo/xwing-unified-data` | `xws` field per ship/pilot/upgrade |
| Backend loaders | `backend/utils/xwing_data/{core,pilots,ships,upgrades,parser}.py` — `get_data_dir`, `load_all_*`, `get_pilot_info`, `get_ship_info`, `get_upgrade_info`, `parse_xws` | `dict[xws_id -> entity]`; pilots carry `ship_xws` |
| Prebuilt frontend manifest | `frontend/static/data-{xwa,legacy}/xwing-data.json`, generated by `frontend/scripts/generate-xwing-data.js` from the same submodule | `ships`/`pilots`/`upgrades` keyed by `xws` |
| Frontend store | `frontend/src/lib/stores/xwingData.svelte.ts` (`getShip`, `getPilot`, `getUpgrade`, `getPilotCountByShip`) | `xws` keys, with pack-suffix fallback |
| Stored squads | Postgres/SQLite `list` table: `list_json` (full XWS), `canonical_signature` (`get_list_key`), `ship_list` (`get_ship_list`) | pilot `id` + upgrade `xws`; `ship` field inside `list_json` |
| Pilot→ship mapping (analytics) | `pilot_ship_mapping` table (`pilot_xws`, `source`, `ship_xws`, `faction`), built by `backend/scripts/populate_pilot_ship_mapping.py` | `pilot_xws` |

Two identity spaces coexist:

1. **XWS ids** (`btanr2ywing`, `zoriibliss`, `wartimeloadout`, `zoriibliss-wartime`, `btanr2wywing`) — used by builders, scrapers, stored `list_json`, and the frontend manifest.
2. **Internal ids** — DB primary keys (`list.id`, `pilot_ship_mapping` rows); legacy pilot→ship mapping. There is no separate m3tacron-invented ship id; XWS ids are used directly everywhere, including in URLs (`/ship/[xws]`, `/pilot/[id]`).

## 4. The pre-split entry and the two new variants

### 4.1 Pre-split (Legacy and pre-split XWA)

- Ship `btanr2ywing` = `BTA-NR2 Y-Wing`, Small, attack 2 / agility 1 / hull 4 / **shields 3**, actions Focus, red Lock, red Barrel Roll, red Boost. (`external_data/xwing-data2-legacy/data/pilots/resistance/bta-nr2-y-wing.json`, and the same file in XWA.)
- Upgrade `wartimeloadout` = `Wartime Loadout`, type Configuration, granted effect "Gain 1 Torpedo slot and 1 Missile slot. Replace your ship ability with the following (Devastating Barrage)." Legacy cost **5**; XWA cost **0**.
- Pilots used the base xws (e.g. `zoriibliss`) and the upgrade was listed under `configuration`.

### 4.2 Post-split (current XWA)

Source files (XWA submodule):

- `external_data/xwing-data2/data/pilots/resistance/bta-nr2-y-wing.json` → `xws: btanr2ywing`
- `external_data/xwing-data2/data/pilots/resistance/bta-nr2-w-y-wing.json` → `xws: btanr2wywing`

The second file (note the doubled `w` in the filename and xws) is the integrated-loadout variant:

| | `btanr2ywing` (no loadout) | `btanr2wywing` (integrated) |
|---|---|---|
| Name | BTA-NR2 Y-Wing | BTA-NR2 Y-Wing |
| Shields | 3 | **5** |
| Actions | Focus, red Lock, red Barrel Roll, red Boost | Focus, **white Lock**, red Barrel Roll, red Boost, **white Reload** |
| Pilot ship ability | Intuitive Interface | **Devastating Barrage** |
| Pilot xws | `zoriibliss`, `tezanasz`, … | `zoriibliss-wartime`, `tezanasz-wartime`, … |
| Points (XWA, examples) | Zorii 11, Teza 8, Wilsa 8, Shasa 9, Lega 8, Aftab 7, Corus 9, C'ai 7, New Republic Patrol 7, Kijimi Spice Runner 9 | Zorii 12, Teza 11, Wilsa 11, Shasa 11, Lega 10, Aftab 10, Corus 11, C'ai 10, New Republic Patrol 10, Kijimi Spice Runner 11 |

The `-wartime` pilot variants (10 of them) all carry `ship: btanr2wywing`. They exist **only in XWA**; the Legacy dataset has neither `btanr2wywing` nor any `-wartime` pilot.

`Wartime Loadout` still exists as an upgrade in XWA (`cost 0`, `standard: true`), still restricted to `ships: ["btanr2ywing"]` and marked `standardized: true`. So both paths to the integrated loadout are still present in the data: the old `btanr2ywing` + `wartimeloadout` upgrade, and the new `btanr2wywing` + `-wartime` pilot.

**Leftover / duplicated rows:** there are no duplicate *ship* rows for one xws, and no duplicate `wartimeloadout` upgrade rows, but two distinct ship entries now share the display name "BTA-NR2 Y-Wing" with different xws (`btanr2ywing`, `btanr2wywing`), and the upgrade that used to represent the variant is still present. That is the source of the "duplicated/confused historical data".

## 5. Other XWA splits of the same class

Searching the XWA submodule for upgrade-turned-into-ship patterns (ship-name duplicates and pilot-id suffixes):

- **Only `BTA-NR2 Y-Wing` has one chassis name mapped to two different ship xws values in the same faction** (`bta-nr2-y-wing.json` → `btanr2ywing`, `bta-nr2-w-y-wing.json` → `btanr2wywing`). All other duplicated ship names are the *same* xws shared across factions (e.g. `fangfighter` Rebel/Scum, `tielnfighter` Rebel/Empire).
- The `-wartime` pilot suffix is unique. Other pilot-id suffixes are pack/box variants (`-battleofyavin`, `-battleoverendor`, `-evacuationofdqar`, `-siegeofcoruscant`, `-armedanddangerous`, `-epicsl`, `-legendsandrelics`, `-ssl`), store/limited variants (`-lsl`), pilot-specific chassis reprints (`caithrenalli-btanr2ywing`, `wedgeantilles-rz1awing`), or cross-faction reprints (`-rebelalliance`, `-scumandvillainy`). None of those bake an upgrade into a new chassis.
- `external_data/xwing-data2/data/quick-builds/resistance.json` references the Y-Wing family; no other chassis shows the "upgrade → dedicated variant" shape.

**Conclusion:** as of this data revision the Y-Wing is the only same-class XWA split, so a targeted fix is sufficient and generalising is not yet justified. This should be re-checked at each XWA points update, since the same treatment could be applied to other `standardized` configurations.

## 6. XWS import/export paths and why codes break

### 6.1 Where XWS is read (import) and written (export)

- **Import from builders:** `backend/scrapers/web_utils.py` `fetch_xws_from_builder` / `_extract_yasb_xws` drives the YASB UI (`button.view-as-text` → `button.select-xws-view` → textarea) and `json.loads` the exported XWS. Scrapers store it verbatim in `list.list_json` (`backend/scrapers/*.py`).
- **Canonical identity (used as the squad "code"):** `backend/utils/list_keys.py` `get_list_key` builds `canonical_signature` from pilot ids + upgrade xws; `get_ship_list` builds `ship_list` from the `ship` fields. `backend/scripts/scrape_tournaments.py` and `migrate_normalize_list.py` persist both.
- **Backend hydration:** `backend/utils/xwing_data/parser.py` `parse_xws` resolves pilot ids and upgrade xws to names.
- **Frontend resolution/export:** `frontend/src/lib/stores/xwingData.svelte.ts` `getPilot`/`getShip`/`getUpgrade` (with pack-suffix stripping); `backend/utils/yasb.py` `xws_to_yasb_url` / `get_xws_string` exist for YASB links/JSON export (not wired to any frontend button found in this repo).
- There is **no user-facing "paste an XWS code" endpoint**: all routers expose GET endpoints only (plus the Ko-fi webhook), so "XWS codes" on the site means the ids inside scraped/stored squads and the `canonical_signature`/`ship_list` derived from them, not a paste-to-import feature.

### 6.2 Reproduced breakages

**(a) Same squadron, two different identities (historical duplication).**
Running the real `get_list_key` / `get_ship_list` on the two XWS forms of the same list:

```
HIST canonical_signature: [{"upgrades": [{"xws": "wartimeloadout"}], "xws": "zoriibliss"}]
NEW  canonical_signature: [{"upgrades": [], "xws": "zoriibliss-wartime"}]
HIST ship_list: btanr2ywing
NEW  ship_list: btanr2wywing
```

The pre-split and post-split encodings of the same real-world squad are counted as two different lists and two different chassis.

**(b) The new chassis has no analytics mapping (data loss).**
`pilot_ship_mapping` (live dev DB) contains **0 rows** for the wartime pilots:

```
         pilot          | list_entries | mapped_rows
------------------------+--------------+-------------
 zoriibliss-wartime     |           71 |           0
 wilsateshlo-wartime    |            6 |           0
 tezanasz-wartime       |            5 |           0
 newrepublicpatrol-wartime |         3 |           0
```

`backend/analytics/ships.py` inner-joins `pilot_ship_mapping psm ON psm.pilot_xws = (p->>'id') AND psm.source = :source`, so every stored squadron that uses a `-wartime` pilot is **dropped from ship analytics** — even though `list.ship_list` contains `btanr2wywing`. The ship page therefore shows stats for old `btanr2ywing` squads but silently omits the new ones.

**(c) The new ship xws has no icon (red "?" in the UI).**
`frontend/src/lib/components/StatIcon.svelte` maps ship xws → glyph chars; it contains `btanr2ywing: "{"` but **no `btanr2wywing`**. `StatIcon` lowercases/strips the type, fails the map lookup, then falls back to the raw string; because its length is > 1 it sets `isError` and renders a red `?`. The XWA font CSS likewise defines `.xwing-miniatures-ship-btanr2ywing` only (`external_data/xwing-miniatures-font/dist/xwing-miniatures.css:1139`).

Confirmed against live production (`m3tacron.com`, read-only, 2026-09-18):

- CSS rule count on the loaded page: `.xwing-miniatures-ship-btanr2ywing` = **1**, `.xwing-miniatures-ship-btanr2wywing` = **0**.
- `GET /ship/btanr2ywing`: header glyph `::before { content: "{" }`, rendered box 14.8 x 24 px.
- `GET /ship/btanr2wywing`: same header glyph `::before { content: none }`, rendered box **0 x 0 px** — the icon is blank, while the page still lists the 10 `-wartime` pilots (`/pilot/zoriibliss-wartime`, …). The ship-detail template also uses `StatIcon type={data.shipXws} isShip` (`frontend/src/routes/ship/[xws]/+page.svelte:633`), which would render the red `?` wherever that block is populated.

**(d) Cross-source resolution fails silently.**
`get_pilot_info("zoriibliss-wartime", DataSource.LEGACY)` → `None`; `get_ship_info("btanr2wywing", DataSource.LEGACY)` → `None`. XWA-created ids cannot resolve when the site is toggled to Legacy, and neither could a future Legacy squad id resolve in XWA.

**(e) Historical payload still resolves to the old semantics.**
`parse_xws` on the historical form returns ship "BTA-NR2 Y-Wing" **and** a `Wartime Loadout` configuration upgrade, while the new form returns the same ship name with no upgrade. The displayed effect is duplicated/ambiguous, matching the user's report.

## 7. Historical / archived squads

Live dev DB (`m3tacron_staging`, read-only):

- `list` rows total: **55,125**
- rows whose `list_json` mentions `wartimeloadout`: **931**
  - of which also mention `zoriibliss`: **638**
  - by tournament format: `amg` 640, `xwa` 177, `ffg` 64, `unknown` 23, `legacy_x2po` 18, `legacy_xlc` 1
- rows mentioning the typo chassis `btanr2wywing`: **76** (all of them also contain a `-wartime` pilot; none use it without one)
- rows mentioning `-wartime`: **76** (57 distinct in `xwa` format, 2 in `amg`)

A representative archived row (`list.id = 129`, 20-point era, format `amg`) stores:

```json
{"id": "zoriibliss", "ship": "btanr2ywing", "points": 4,
 "upgrades": {"talent": ["deadeyeshot"], "torpedo": ["advprotontorpedoes"],
              "astromech": ["r4astromech"], "configuration": ["wartimeloadout"]}}
```

It currently resolves to the non-wartime `btanr2ywing` chassis with Intuitive Interface **plus** a `Wartime Loadout` configuration upgrade — i.e. the old semantics, even though that configuration is now the integrated variant.

A representative new-format row (`list.id = 7538`, `xwa`, 50-point era) stores:

```json
{"id": "zoriibliss-wartime", "name": "zoriibliss-wartime", "ship": "btanr2wywing", "points": 12,
 "upgrades": {"configuration": ["integratedsfoils"]}}
```

Sample commands used (read-only):

```sql
SELECT count(*) FROM list;
SELECT count(*) FROM list WHERE list_json::text LIKE '%wartimeloadout%';
SELECT count(*) FROM list WHERE list_json::text LIKE '%wartimeloadout%' AND list_json::text LIKE '%zoriibliss%';
SELECT t.format, count(DISTINCT l.id)
FROM list l JOIN playerstanding ps ON ps.list_id=l.id JOIN tournament t ON t.id=ps.tournament_id
WHERE l.list_json::text LIKE '%wartimeloadout%' GROUP BY 1;
SELECT p->>'id', count(*), count(psm.pilot_xws)
FROM list l JOIN jsonb_array_elements(l.list_json::jsonb->'pilots') p ON true
LEFT JOIN pilot_ship_mapping psm ON psm.pilot_xws=(p->>'id') AND psm.source='xwa'
WHERE p->>'id' LIKE '%-wartime' GROUP BY 1;
```

No writes were issued against the database.

## 8. Impact assessment

1. **Historical duplication:** the same real squad is counted twice (old + new XWS), and historical rows still show the now-integrated upgrade as a separate card.
2. **Silent analytics loss:** 76 stored squads (85 wartime pilot entries) never reach the ship analytics because `pilot_ship_mapping` has no `-wartime` rows. This is the most damaging, because nothing errors — the numbers are just wrong.
3. **Missing ship icon:** both Y-Wing chassis share the display name, but only `btanr2ywing` has a glyph; `btanr2wywing` renders a blank header icon (confirmed live) and a red `?` wherever the `StatIcon` fallback path is used.
4. **Two ship pages for one chassis:** `/ship/btanr2ywing` and `/ship/btanr2wywing` split popularity/stats that users read as one ship; the shipped `frontend/static/data-xwa/xwing-data.json` also contains both.
5. **Cross-source source toggle:** XWA `-wartime` ids return `None` under Legacy, so those entries render as raw ids.

## 9. Candidate minimal fixes (NOT implemented)

Ranked by value / risk. Any chosen option still needs the user's decision; the confirmed product authority is official XWA ids and points for the two variants, the old ship id must remain a deprecated alias, and historical migration must be contextual (a squad that also held the `Wartime Loadout` upgrade becomes the integrated variant, otherwise the plain variant).

1. **Add an alias/fallback layer for the wartime ids (lowest risk, no data rewrite).**
   - Files: `backend/utils/xwing_data/pilots.py` (`get_pilot_info`), `ships.py` (`get_ship_info`), `upgrades.py` (`get_upgrade_info` if needed), `frontend/src/lib/stores/xwingData.svelte.ts` (`getPilot`/`getShip`).
   - Treat `zoriibliss-wartime` as the wartime variant and resolve `-wartime` ids; expose `btanr2ywing` as a deprecated alias that can point at either variant depending on context. Removes the `None` results and raw-id display.
   - Trade-off: does not by itself fix analytics or historical counting; needs a canonical direction so counts do not double.

2. **Refresh `pilot_ship_mapping` and make it resilient (high value, low risk).**
   - Files: `backend/scripts/populate_pilot_ship_mapping.py` (re-run), or add the missing rows. Both wartime pilots (`btanr2wywing`) and the base mapping must be present.
   - Trade-off: one-off data operation; must be scheduled after every XWA data bump, otherwise the drift recurs. Could be paired with a startup/deploy check.

3. **Contextual historical migration of `list_json` / `canonical_signature` / `ship_list` (matches the product decision; medium risk).**
   - Files: a new migration under `backend/scripts/` (pattern: `migrate_*.py`) plus `backend/utils/list_keys.py` if the canonical form should prefer the wartime id.
   - For rows containing a `-wartime`-less pilot **and** `wartimeloadout`, rewrite the pilot to the integrated variant and drop the upgrade; otherwise leave as the plain variant. Preserve the old id as an alias so old links keep resolving.
   - Trade-off: irreversible in place without a backup; changes stored historical data; must be idempotent and reversible.

4. **Add the missing icon glyph (trivial, cosmetic).**
   - Files: `frontend/src/lib/components/StatIcon.svelte` (map `btanr2wywing` to the same glyph as `btanr2ywing`), and/or regenerate/alias the font CSS.
   - Trade-off: cosmetic only; does not fix data.

5. **Generalise split handling (defer).**
   - Files: `backend/utils/xwing_data/*`, scrapers, frontend store; a suffix/alias registry for upgrade-turned-ship splits.
   - Trade-off: unjustified today because the Y-Wing is the only same-class split in the current XWA revision; adds abstraction without a second case.

Recommended order if the user approves a fix: (2) mapping repair + (1) alias layer first (restores correctness with no data rewrite), then (3) historical migration, then (4) icon. (5) only when a second same-class split appears.

## 10. Verification log (commands actually run)

```bash
# Submodule data available to the backend
git submodule update --init --recursive external_data

# Identify the two Y-Wing ship files and their xws/points
python3 -c "import json; d=json.load(open('external_data/xwing-data2/data/pilots/resistance/bta-nr2-w-y-wing.json')); print(d['xws'],[ (p['xws'],p['cost']) for p in d['pilots']])"

# Reproduce backend XWS parsing / lookup behaviour (real code, real data)
python3 /tmp/xws_repro.py   # see description below
```

`/tmp/xws_repro.py` (temporary, not committed) imported `backend.utils.xwing_data.parser.parse_xws`, `pilots.get_pilot_info`, `ships.get_ship_info`, `upgrades.get_upgrade_info` and `utils.list_keys.get_list_key`/`get_ship_list`, and printed the outputs quoted in §6 and §7. Observed, in order:

```
HISTORICAL (old) parse_xws -> ship "BTA-NR2 Y-Wing", upgrades include "Wartime Loadout"
NEW (post-split) parse_xws -> ship "BTA-NR2 Y-Wing", no Wartime Loadout upgrade
PILOT zoriibliss-wartime XWA: name=Zorii Bliss ship_xws=btanr2wywing cost=12
PILOT zoriibliss-wartime LEGACY: None
SHIP btanr2ywing XWA: shields 3
SHIP btanr2wywing XWA: shields 5
SHIP btanr2wywing LEGACY: None
UPGRADE wartimeloadout XWA cost: {'value': 0}; LEGACY cost: {'value': 5}
```

Live browser check of the icon breakage (read-only, production `m3tacron.com`, via the agent browser):

```
open https://m3tacron.com/ship/btanr2ywing
eval -> ::before content "{" ; box 14.8 x 24 ; pilot links 10
open https://m3tacron.com/ship/btanr2wywing
eval -> ::before content none ; box 0 x 0 ; pilot links 10
        (/pilot/zoriibliss-wartime, /pilot/tezanasz-wartime, ...)
eval -> styleSheet rules matching xwing-miniatures-ship-btanr2ywing = 1,
        ...btanr2wywing = 0
```

Repository checks run for this pass: none needed, because no code changed. A `git status` after writing this report shows only the new report file (and the populated submodule working tree, whose gitlink commit is unchanged).

## 11. Explicitly not verified / limitations

- **No local app stack was started and no API process was executed.** Backend behaviour was reproduced by importing the real modules and calling them directly; DB facts come from read-only SQL against the live dev database; the icon fact comes from a read-only browser check against production. The `StatIcon` red-`?` branch was **not** observed live, because the blocks that use it render only when the ship has analytics rows, and `btanr2wywing` has none (itself finding §6(b)); it is derived from the component code. A future fix PR should confirm the red-`?` case locally once the mapping is repaired.
- **The live dev database is shared and evolving.** Counts in §7 are a snapshot at 2026-09-18 and will drift.
- **Only the XWA revision pinned by submodule commit `3bb6b58` was inspected.** A later XWA update could add more same-class splits.
- **`pilot_ship_mapping` staleness source was inferred** from its contents versus `load_all_pilots` (the mapping file has no `-wartime` rows); the last execution of the populate script was not audited.

## 12. Non-destructive confirmation

This pass changed one file only: this report. No source, schema, seed, migration, data or database row was modified; no PR was merged; nothing was deployed to production. The live checks above were read-only GETs/browser reads against production; the SQL was read-only.

## 13. Decisions (phase-1 implementation)

Recorded during the phase-1 implementation that landed on the same branch/PR as this report.

1. **History is resolved at read time; no rows are rewritten.** The deprecated-ship alias resolver (`backend/utils/xwing_data/aliases.py`) maps a pre-split reference contextually at lookup time. No `UPDATE`, no backfill, no migration, no schema or vendored-data change was made. Old XWS codes and old links keep resolving to the variant they represent, and the old form and new form of one squad collapse to one `canonical_signature` / `ship_list`.
2. **`pilot_ship_mapping` is kept but becomes a derived, regenerable cache.** For the later analytics phase, the table should be treated as a projection produced by the resolver (pilot id -> resolved `ship_xws`), not as a source of truth. It can be rebuilt from the manifest + alias map at any time, so analytics can rejoin on the resolved identity without a historical data migration.
3. **Open question for the product owner — Legacy-source resolution.** `btanr2ywing` + `wartimeloadout` resolves to `btanr2wywing` only in the XWA source; the Legacy dataset has no integrated variant, so the resolver deliberately leaves Legacy references untouched (`btanr2ywing`, pilot `zoriibliss`). This may be correct if the Legacy source represents the pre-split points system, but it is not fixed speculatively. Product decision needed: should Legacy references also advertise the integrated identity, or stay pre-split?
4. **Still deferred (unchanged from §9):** the `pilot_ship_mapping` refresh/analytics rejoin, any materialisation or migration of historical rows, and the residual `wartimeloadout` upgrade row beyond what read-time absorption already achieves.

## 14. Phase-1 scope addition — display labels and the wartime card

Added after a product review of the first phase-1 cut. Two questions: the two chassis must be distinguishable by eye, and the wartime chassis must be checked against the Delta-7B "own card" pattern.

### 14.1 Display labels (Requirement A)

The vendored data gives both chassis the same `name` (`BTA-NR2 Y-Wing`), so every human-facing surface printed a shared string. The label difference is applied in the layer that derives the human name; no id was renamed.

| Chassis xws | Old derived name | New label |
|---|---|---|
| `btanr2ywing` (plain) | `BTA-NR2 Y-Wing` | `Y-wing` |
| `btanr2wywing` (integrated) | `BTA-NR2 Y-Wing` | `Y-wing (Wartime Loadout)` |

Where the labels are defined and applied:

- **Definition (backend):** `backend/utils/xwing_data/aliases.py` — each `ShipSplitAlias` entry now carries `deprecated_label` / `variant_label`; `ship_label_overrides()` and `ship_display_name()` expose them. One map entry per future split.
- **Definition (frontend):** `frontend/src/lib/data/shipLabels.json` (mirror of the Python map, because the manifest generator is a Node script that cannot import the Python module) plus `frontend/src/lib/data/shipLabels.ts` (`getShipDisplayName`).
- **Applied (backend):** `backend/utils/xwing_data/ships.py` `load_all_ships` (`name`) and `backend/utils/xwing_data/pilots.py` `load_all_pilots` (`ship`). Every backend-rendered name path derives from these two loaders: `/api/ships`, `/api/ships/all`, `/api/ship/{xws}` `info.name`, squadron `ship_name`, `parse_xws` ship names, ship filter options.
- **Applied (frontend):** `frontend/scripts/generate-xwing-data.js` writes the label into `ships[xws].name` of the prebuilt manifests (both `data-xwa` and `data-legacy` were regenerated; the only diff is the two Y-Wing `name` fields); `frontend/src/lib/stores/xwingData.svelte.ts` `getShip()` passes the name through the label layer; `frontend/src/lib/stores/filters.svelte.ts` labels the active ship-filter chip with the display name.

Unchanged on purpose: the xws ids `btanr2ywing` / `btanr2wywing`, the pilot `-wartime` suffix, `pilot.xws`, `pilot.ship_xws`, and all stored `list_json`. Only the derived string changes.

### 14.2 Is there a full wartime card? Delta-7B as the reference (Requirement B)

**Reference pattern — Delta-7 vs Delta-7B.** Two separate ship files, two xws, two statlines, each with its own pilots carrying their own `shipAbility`:

- `external_data/xwing-data2/data/pilots/galactic-republic/delta-7-aethersprite.json` — `xws: delta7aethersprite`, stats attack 2 / agility 3 / hull 3 / shields 1, actions Focus(white) Evade(purple) Lock(white) Barrel Roll(white) Boost(white). Pilot `jediknight` has `shipAbility: {name: "Fine-tuned Controls", ...}`.
- `external_data/xwing-data2/data/pilots/galactic-republic/delta-7b-aethersprite.json` — `xws: delta7baethersprite`, stats attack 3 / agility 2 / hull 3 / shields 3, same action bar. Pilot `jediknight-delta7baethersprite` has its own `shipAbility: {name: "Fine-tuned Controls", ...}`. (In this data revision the ability text happens to match the base chassis, but the pilot ids and statline are the variant's own.)
- Legacy (`external_data/xwing-data2-legacy/data/pilots/galactic-republic/`) contains **only** `delta-7-aethersprite.json`; the Delta-7B is XWA-only, same as the integrated Y-Wing.

Served/rendered by: `backend/utils/xwing_data/ships.py::load_all_ships` flattens each ship file into a `dict` keyed by `xws`; `frontend/scripts/generate-xwing-data.js` emits `ships[xws]` / `pilots[xws]`; `frontend/src/routes/ship/[xws]/+page.svelte` reads `xwingData.getShip(data.shipXws)?.stats` for the statline. `shipAbility` was present in the manifest but rendered nowhere — not for the Delta-7B either.

**Target — verified against the source files.**

- `external_data/xwing-data2/data/pilots/resistance/bta-nr2-y-wing.json` — `xws: btanr2ywing`, `name: "BTA-NR2 Y-Wing"`, stats attack 2 / agility 1 / hull 4 / **shields 3**, actions Focus(white) Lock(**red**) Barrel Roll(red) Boost(red). Pilot `zoriibliss` has `shipAbility: {name: "Intuitive Interface", text: "After you perform an action added to your action bar by a [Talent], [Illicit], or [Modification] upgrade, you may perform a [Calculate] action."}`.
- `external_data/xwing-data2/data/pilots/resistance/bta-nr2-w-y-wing.json` — `xws: btanr2wywing`, `name: "BTA-NR2 Y-Wing"` (same name), stats attack 2 / agility 1 / hull 4 / **shields 5**, actions Focus(white) Lock(**white**) Barrel Roll(red) Boost(red) **Reload(white)**. Pilot `zoriibliss-wartime` (and the other 9 `-wartime` ids) has `shipAbility: {name: "Devastating Barrage", text: "While you perform a [Torpedo] or [Missile] attack, if the defender is in your [Bullseye Arc], your [Critical Hit] results cannot be cancelled by [Evade] results."}`.
- `external_data/xwing-data2-legacy/data/pilots/resistance/` contains only `bta-nr2-y-wing.json` (`btanr2ywing`, shields 3). There is no `btanr2wywing` and no `-wartime` pilot in Legacy.

**Conclusion: the XWA source DOES contain a full card for the wartime variant** — its own ship entry, its own statline (attack 2 / agility 1 / hull 4 / shields 5) and its own pilot chassis ability (`Devastating Barrage`) — in exactly the shape the Delta-7B uses. Legacy does not.

What was therefore implemented (no data synthesised):

1. The statline was already rendered from the variant's own `ships[btanr2wywing].stats`; it needs no change (`/ship/btanr2wywing` shows shields 5, `/ship/btanr2ywing` shows shields 3). Verified live below.
2. The chassis ability is now rendered on the ship detail page for any ship whose data has one: `xwingData.getShipAbility(xws)` (`frontend/src/lib/stores/xwingData.svelte.ts`) reads the `shipAbility` shared by the chassis' pilots, and `frontend/src/routes/ship/[xws]/+page.svelte` shows it in the hero. The wartime variant shows `Devastating Barrage`, the plain chassis `Intuitive Interface`, and the Delta-7B reference shows `Fine-tuned Controls`. When the data has no ability the block is simply absent — nothing is invented or copied across chassis.

### 14.3 Open questions for the product owner (phase-1 scope addition)

1. **Legacy source (unchanged from §13.3):** should Legacy references advertise the integrated identity, or stay pre-split?
2. **Wartime card in Legacy:** Legacy has no integrated Y-Wing entry at all. If the Legacy toggle is expected to show the wartime variant as a distinct card, that data does not exist upstream and must not be synthesised here.
3. **Pilot names in the split:** both chassis' pilots keep the same display name (`Zorii Bliss`); only the ship label differs. Confirm that is the intended presentation.
