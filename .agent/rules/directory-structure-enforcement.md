---
trigger: always_on
---

Directory Structure Enforcement
Do NOT add new files or folders to the project root.
All new content (scripts, components, pages, logic) must be placed within the `/m3tacron/` subfolder or its subfolders.
Test scripts and output **must** be placed in the existing `test` folder and after being used they must be deleted.